# Repository instructions

- Write documentation, code comments, setup guidance, and other project prose in English by default.
- Non-English text is allowed only when it is required runtime input, such as quota-error detection patterns or test fixtures.
- Keep X credentials, OAuth tokens, cooldown state, and machine-specific private paths outside the repository.
- Preserve dry-run as the default and require explicit authorization before any live post.
